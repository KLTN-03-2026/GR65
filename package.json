{
  "name": "ai-recruit-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "AI Recruitment Platform - Local Development",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"yellow,cyan\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:ai": "cd ai_service && python main.py",
    "dev:all": "concurrently -n \"AI,BACKEND,FRONTEND\" -c \"magenta,yellow,cyan\" \"npm run dev:ai\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
